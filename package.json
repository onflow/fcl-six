{
  "workspaces": [
    "./six-*/*",
    "./staking-collection/*",
    "./topshot/*",
    "./usdc/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "changeset": "changeset",
    "release": "npm run build && npm run changeset publish"
  },
  "dependencies": {
    "@changesets/cli": "^2.26.2"
  },
  "devDependencies": {
    "lerna": "^7.1.3"
  }
}
